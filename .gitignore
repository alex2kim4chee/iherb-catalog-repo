# macOS
.DS_Store

# Дубликаты, которые создаёт синхронизация iCloud в папке Documents:
# «page.html» -> «page 2.html». В сгенерированном каталоге таких имён быть
# не может, поэтому шаблон безопасен и защищает от публикации мусора.
* 2.*
* 3.*
